services:
  cassandra:
    image: cassandra:5.0
    container_name: practice5-cassandra
    ports:
      - "9042:9042"
    environment:
      MAX_HEAP_SIZE: 512M
      HEAP_NEWSIZE: 128M
    volumes:
      - cassandra_data:/var/lib/cassandra

volumes:
  cassandra_data:
